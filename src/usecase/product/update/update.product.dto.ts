export default interface InputUpdateProductDTO {
  id: string;
  name: string;
  price: number;
  type: string;
}

export interface OutputUpdateProductDTO {
  id: string;
  name: string;
  price: number;
}
